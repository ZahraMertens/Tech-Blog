<!-- Loop over each object in the projects array -->
{{#each posts}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/project/{{id}}">{{title}}</a>
    </h2>
    <p>
      {{post_content}}
    </p>
    <p>Created by on {{date_created}}</p>
  </div>
</div>
{{/each}}